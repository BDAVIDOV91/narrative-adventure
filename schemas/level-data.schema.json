{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://narrative-adventure.local/schemas/level-data.schema.json",
  "title": "Level data",
  "description": "One playable location. Everything a level needs that is not code: where the player starts, where the puzzle markers are, which reusable puzzle type each marker opens, and which precomputed astronomy data it reads.",
  "type": "object",
  "required": ["id", "displayName", "mode", "unlockThreshold", "map", "markers"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Stable level id, kebab-case. Used as the localStorage progress key."
    },
    "displayName": {
      "$ref": "#/$defs/contentKey",
      "description": "Content key, never literal text. Bulgarian strings live in content/bg/."
    },
    "mode": {
      "enum": ["guided", "open"],
      "description": "guided = linear tutorial (Earth, Moon). open = free exploration with scattered markers."
    },
    "unlockThreshold": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Fraction of this level's markers that must be solved to unlock the next page. 1.0 for guided levels, 0.7 for open ones — the brief's forgiving gate, kept as data so both kinds share one code path."
    },
    "map": {
      "type": "object",
      "required": ["width", "height", "spawn"],
      "additionalProperties": false,
      "properties": {
        "width": { "type": "integer", "minimum": 1 },
        "height": { "type": "integer", "minimum": 1 },
        "spawn": { "$ref": "#/$defs/point" }
      }
    },
    "markers": {
      "type": "array",
      "minItems": 1,
      "description": "Puzzle stations. The brief calls for roughly 3-4 per level.",
      "items": { "$ref": "#/$defs/marker" }
    }
  },
  "$defs": {
    "contentKey": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9.-]*$",
      "description": "ASCII key resolved through src/shared/content.ts. A literal Cyrillic string here is a bug."
    },
    "point": {
      "type": "object",
      "required": ["x", "y"],
      "additionalProperties": false,
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" }
      }
    },
    "marker": {
      "type": "object",
      "required": ["id", "position", "puzzle", "label"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
        "position": { "$ref": "#/$defs/point" },
        "puzzle": {
          "enum": [
            "rotate-match",
            "connect-the-dots",
            "parallax-compare",
            "zoom-split-star",
            "trajectory-match"
          ],
          "description": "One of the five reusable puzzle types. Each is built once and reskinned per planet — a sixth value here means a one-off that needs its own maintenance forever."
        },
        "label": { "$ref": "#/$defs/contentKey" },
        "config": {
          "type": "object",
          "description": "Puzzle-type-specific settings. Validated per type by data/scripts/validate-levels.py."
        },
        "dataRef": {
          "type": "string",
          "description": "The seam between Python output and the game: a JSON pointer into a file under data/generated/, e.g. 'orbital-positions.json#/earth'.",
          "pattern": "^[a-z0-9-]+\\.json#(/[A-Za-z0-9_-]+)*$"
        },
        "reward": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "unlocks": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Ids of things in the world that react when this is solved — a path lighting up, a door opening."
            },
            "fact": {
              "$ref": "#/$defs/contentKey",
              "description": "The short fact shown once the puzzle is solved."
            }
          }
        }
      }
    }
  }
}
