/// <reference types="vite/client" />

// Vite's ambient types, for `import.meta.glob` and friends. Kept as its own
// file rather than a compilerOptions.types entry, which would switch off the
// automatic @types/* discovery that @types/node and @types/three rely on.
